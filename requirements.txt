pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
yfinance>=0.2.40

# Optional, only needed for China A-share tickers (6-digit codes).
# Install manually if you plan to analyze A-shares: pip install akshare
